node_modules/
dist/
.wrangler/
*.local
.DS_Store
app/e2e/shots/

# Secrets. The OJP key and device token live here in local development and in
# `wrangler secret` in production — never in the app bundle, which is public.
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
!.env.example

# Python bytecode from the Blender build scripts.
__pycache__/
*.pyc
