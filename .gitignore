node_modules/
dist/
.wrangler/
*.local
.DS_Store
app/e2e/shots/

# Secrets. The OJP key and device token live here in local development and in
# `wrangler secret` in production — never in the app bundle, which is public.
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
!.env.example

# Blender project working files
reference/frames_overview/
reference/gap/
reference/shots/
project/previews/
project/renders/
project/source/*.blend
project/koenigsegg_final.blend

# Blender backup files
*.blend1
*.blend2
__pycache__/
*.pyc
# Keep the encoded videos and a set of reference-vs-render comparisons; skip
# the bulk frame dumps and the PNG sequence.
output/*
!output/*.mp4
project/previews/*
!project/previews/compare/

# Fetched by project/scripts/bootstrap.sh
project/assets/hdri/
