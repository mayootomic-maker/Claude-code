# Windows validation register

Everything that compiles and unit-tests on Linux but has **never executed against real
Windows**. Nothing on this list may be described as "working".

This file exists because a marker that nothing collects gets ignored within a few sessions.
`REQUIRES-WINDOWS-VALIDATION` in a council transcript is not a record — a row here is.

`/council-prerelease` counts open CRITICAL rows. **READY-FOR-WINDOWS-VALIDATION is impossible
while any CRITICAL row is open**, and `SHIP` is not a verdict this environment can produce at
all.

## How to add a row

| Column | Meaning |
|---|---|
| **What is unverified** | The specific behaviour, not the component |
| **Windows test that resolves it** | Concrete enough that someone else could run it |
| **Protects** | The file or ADR that depends on this being true |
| **Severity** | CRITICAL / HIGH / MEDIUM / LOW |

Severity is about consequence-if-wrong, not effort-to-test:

- **CRITICAL** — could leave the user's system in a modified state, lose their data, or
  produce a confidently wrong diagnosis
- **HIGH** — a core feature silently does not work
- **MEDIUM** — degraded behaviour with a visible symptom
- **LOW** — cosmetic or convenience

## Open

| What is unverified | Windows test that resolves it | Protects | Severity |
|---|---|---|---|
| An **unelevated** PresentMon SDK client can actually consume the service. The service's control pipe is ACL'd to Authenticated Users in source, but this has never been exercised end to end. | Install the PresentMon service as admin; from a standard-user session, `pmOpenSession` + `pmRegisterFrameQuery` + `pmConsumeFrames` against a running game. | The entire no-elevation deployment story. If false, every user must join Performance Log Users and sign out. | CRITICAL |
| `PowerSetActiveOverlayScheme` succeeds for a standard (non-admin) user. It is an **undocumented** powrprof export. | Call it as a standard user to set Best Performance; read back with `PowerGetEffectiveOverlayScheme`; confirm the change and the restore. | The power-mode optimization. If it needs admin, that optimization is gated behind elevation or dropped. | HIGH |
| `\Processor Information(*)\Processor Frequency` reports **live** frequency rather than nominal, on Intel HWP (12th gen+) and AMD PBO. | Read the counter under sustained load and at idle on both vendors; compare against a known-good tool. | CPU clock-collapse diagnosis. If nominal, `% Processor Performance` derivation is the only path. | HIGH |
| `% Processor Performance` is meaningful per-logical-processor on **hybrid P/E-core** CPUs, and what "nominal" means for E-core instances. | Read per-instance on a 12th-gen-or-later Intel part under mixed load. | Effective-clock derivation and single-thread bottleneck detection on every modern Intel CPU. | HIGH |
| Real PDH cost at our sampling rates. `\GPU Engine(*)` can enumerate hundreds of instances during a game. | Measure `PdhCollectQueryData` wall time and CPU at 1 Hz and 10 Hz for: 32 `Processor Information` instances; `\Process(*)` on a ~300-process box; `\GPU Engine(*)` with a game running. | The whole active-monitoring CPU budget (≤1.0% total). | CRITICAL |
| `PdhAddEnglishCounter` + `PdhExpandWildCardPath` works for `GPU Engine(*)` on **non-English** Windows. | Run the counter enumeration on German or Japanese Windows. | Per-process GPU attribution for every non-English user. | HIGH |
| ETW manifest-provider contention. Only 8 sessions may enable a manifest provider; DXGI/DxgKrnl/DWM are all manifest-based. | Run FrameDoctor alongside RTSS, MSI Afterburner, GeForce Experience, Xbox Game Bar and CapFrameX simultaneously; observe the failure mode and whether it is detectable. | Graceful degradation when we lose the race. Silent failure here looks like "no stutters detected". | CRITICAL |
| Anti-cheat reaction to ETW-based frame collection. | Run against EAC, BattlEye and Vanguard titles; observe whether collection fails, and whether the process is flagged. | Whether FrameDoctor works at all on competitive titles, and whether it can get a user banned. | CRITICAL |
| PawnIO device access after install: elevation needed only to install, or also to open and ioctl. | As a standard user with PawnIO already installed, open the device and read a CPU temperature via LibreHardwareMonitorLib. | Whether Tier 2 telemetry needs a per-launch elevation prompt. | MEDIUM |
| ADLX read-only `IADLXGPUMetrics` works without elevation. | Query GPU metrics as a standard user on an AMD GPU. | AMD GPU telemetry parity with NVIDIA. | MEDIUM |
| `\Thermal Zone Information(*)\Throttle Reasons` is populated on real desktop hardware. | Read the counter on desktop Intel and AMD systems under thermal load. | CPU thermal-throttle diagnosis without a kernel driver. | HIGH |
| WPF shell actually launches and renders. It compiles here but has never executed. | Launch the built shell on Windows 10 and Windows 11. | Every visual verdict made from a web-frontend screenshot rather than the real shell. | HIGH |
| The published `framedoctor.exe` launches on Windows. It is a valid PE32+ binary and the byte-identical trimmed configuration runs correctly on Linux, but no Windows machine has executed it. | Run `framedoctor.exe run-all` on Windows 10 and 11. Confirm all six scenarios report and the trimmed single-file bundle extracts. | The whole self-contained, trimmed, single-file publish path chosen in ADR 0001. | MEDIUM |
| **FrameDoctor's own frame-time impact.** Invariant 8 is currently asserted by self-instrumentation, which cannot see the ETW logger thread's kernel CPU or a GC pause. | Deterministic in-game benchmark, 3 runs with FrameDoctor off and 3 with it monitoring. Require Δp99 frame time ≤ 0.3 ms and Δ1%-low ≤ 1 % at 95 % confidence. | `docs/architecture/performance-budget.md` — this is the only test that actually tests invariant 8. | CRITICAL |
| A sustained `% Processor Performance` drop caused by a **normal all-core boost-bin change** is indistinguishable from thermal/power throttling using Tier 0 data alone. | On a desktop Intel and AMD part, ramp 1 → all cores at fixed cooling; record per-instance `% Processor Performance`, `% Processor Utility` and active-core count. Then repeat under deliberate thermal limitation. Determine whether any Tier-0 combination separates the two. | `cpu.throttle.state` and the product's headline thermal-throttling claim. If it does not separate, Tier 0 must never emit "thermal". | CRITICAL |
| `StartTraceW` / `EnableTraceEx2` succeed for an **unelevated process** whose account is a filtered administrator and a member of Performance Log Users. | On a default consumer single-admin machine, add the account to Performance Log Users, sign out/in, and run the bundled PresentMon CLI without elevation. | The entire v1 (bundled CLI) frame-telemetry route. If false, v1 needs a UAC prompt per session and is not shippable. | CRITICAL |
| PresentMon's realtime session never sets `FlushTimer`, so ETW applies its documented **1-second** real-time default. End-to-end freshness of CLI-sourced frames is therefore unknown and may be ~1 s. | Timestamp a known present (QPC) and the moment its CSV row is read from the child's stdout; histogram over 10 000 frames at 60 / 144 / 240 fps. Repeat via the service with `pmSetEtwFlushPeriod(50)`. | The `Event detected → visible on Live timeline ≤ 400 ms` budget line and the `IFrameSource.LatencyClass` contract. | HIGH |
| `EnumerateTraceGuidsEx(TraceGuidQueryInfo)` can be called **unelevated**, and `TRACE_PROVIDER_INSTANCE_INFO.EnableCount` really reflects the 8-sessions-per-manifest-provider limit. | As a standard user, query DXGI / DxgKrnl / Dwm-Core / Win32k. Start sessions until enable fails with `ERROR_NO_SYSTEM_RESOURCES` (1450) and confirm `EnableCount` reached 8 beforehand. | The pre-flight check that turns ETW slot exhaustion into an explained `Unavailable(...)` instead of a mystery failure. | HIGH |
| `\GPU Engine(*)` **instance names** (`pid_…_luid_…_engtype_3D`) are not localized on non-English Windows. | Enumerate the instances on German or Japanese Windows and confirm the `pid_`/`engtype_` grammar is byte-identical to en-US. | Per-process GPU attribution and game detection. Distinct from the `PdhAddEnglishCounter` row: this is about the instance strings, not the object/counter names. | HIGH |
| Adding the user to **Performance Log Users** requires a sign-out to take effect, and `CheckTokenMembership(WinBuiltinPerfLoggingUsersSid)` reports the pre-sign-out state correctly. | Add the account during install, do not sign out, run the membership check and then the CLI; then sign out/in and repeat. | The install flow. Without this the user sees an unexplained failure straight after a successful install. | HIGH |
| Win32k input tracking is **on by default** in the PresentMon CLI. Its event-rate cost with a 1000 Hz gaming mouse is unmeasured. | Capture with and without `--no_track_input` at 1000 Hz mouse polling; compare `EventsLost`, `BufferFillPct` and child-process CPU. | The decision to default to `--no_track_input`, and the click-to-photon feature's cost. | MEDIUM |
| The PresentMon service's `PM_METRIC_CPU_TEMPERATURE` / `PM_METRIC_CPU_POWER` may be available on consumer hardware **without any driver of ours**. | With the service installed, `pmGetIntrospectionRoot` then poll both metrics on consumer Intel and AMD desktops and laptops; record availability per SKU. | Tier 2's scope. If available, CPU temperature stops being driver-gated and the headline diagnosis becomes Tier-0/1 reachable. | MEDIUM |
| PresentMon's `[ETW Status] BufferFillPct=… EventsLost=… BuffersLost=…` health line: which stream it goes to, and whether `--no_console_stats` suppresses it. | Run the CLI with `--output_stdout`, redirect stdout and stderr separately, and confirm where the health line lands, with and without `--no_console_stats`. | `TelemetryHealth` for the v1 source. Without it, dropped ETW events are reported as real stutters. | MEDIUM |
| The ETW session's kernel buffer pool (PresentMon hardcodes 64 KB × 256–1024 buffers = **16–64 MB non-paged**) is real, per-session, and not counted in any process working set. | Read `NumberOfBuffers` from `ControlTrace(EVENT_TRACE_CONTROL_QUERY)` while capturing, and watch non-paged pool in Performance Monitor. | The memory budget's honesty. It currently accounts for working set only. | LOW |
| **Explanation rate** — the share of real detected stutters that reach a *named cause* at medium-or-better confidence on Tier 0 telemetry alone. | Run 10 real gaming sessions with Tier 0 sources only. Count detected stutters; count those reaching a named diagnosis at >= medium confidence. Report the ratio. | The product's reason to exist. If this is low, v1 is a stutter *counter*, not a diagnostician. Gates the whole v1 scope. | CRITICAL |
| Whether 1-4 Hz Tier 0 counter sampling can resolve a 20-200 ms stutter well enough to attribute a cause at all. | Inject a known ~150 ms stall (CPU hog thread; forced hard-fault paging) during a captured session; check whether any counter series shows a correlate at the natural sample rate. Repeat with a pre-event high-rate ring buffer retained on stutter and measure the attribution improvement. | `docs/architecture/telemetry-model.md` (Rates); the event inspector; the diagnostic engine. | CRITICAL |
| `SetProcessInformation(hProcess, ProcessPowerThrottling, ...)` succeeds against **another same-user process from an unelevated caller**, and the documented reset (`ControlMask=0, StateMask=0`) observably restores system-managed QoS. | On a standard (non-admin) account, from an unelevated build: apply EcoQoS to a running `notepad.exe`; confirm the change in Task Manager's Power-usage column; apply the reset and confirm it returns. Repeat against an elevated target and an anti-cheat-protected game and record the exact `GetLastError`. | The only system mutation shipping in v1, and the entire "no elevated component" privilege model. | CRITICAL |
| The write-ahead journal ordering holds under power loss: there is **no state in which a mutation is applied but its journal entry is absent**. | Instrumented build that applies EcoQoS immediately after `MoveFileEx(MOVEFILE_REPLACE_EXISTING\|MOVEFILE_WRITE_THROUGH)`. Hard-power-cut the machine 20 times at randomised points in the apply path. On each boot run `FrameDoctor.exe --reconcile-and-exit` and confirm no process is left throttled without a matching entry. | Invariant 4; rollback doctrine points 2 and 4. | CRITICAL |
| Job-object limits actually stop applying to an already-assigned third-party process once the last job handle closes (asserted in `docs/research/windows-internals.md` §7.5). | Create a job with `JOB_OBJECT_CPU_RATE_CONTROL_ENABLE\|WEIGHT_BASED`, assign a running process, `TerminateProcess` the creator, then confirm by measured CPU and `QueryInformationJobObject` that the process is no longer constrained. | The "changes that die with the process need no rollback machinery" class, and the v1 deferral of job-object CPU capping. | HIGH |
| `PowerSetRequest(PowerRequestDisplayRequired \| PowerRequestSystemRequired)` is released when the holding process is killed with `TerminateProcess`, not only on clean exit. | Take both requests, confirm them in `powercfg /requests`, `TerminateProcess` the holder from Task Manager, confirm both entries disappear. | Classifying the gamepad display-keep-awake feature as self-reverting (no journal entry). | HIGH |
| `QueryInterruptTime` minus `QueryUnbiasedInterruptTime` reports elapsed sleep correctly on **Modern Standby / connected standby**, not only on S3. | On a Modern Standby laptop and on a handheld: record both clocks, suspend for a measured 5 minutes, resume, confirm the difference equals the sleep duration within one clock tick. | Suspend detection and the discontinuity rule in `docs/architecture/telemetry-model.md`. | HIGH |
| The PresentMon service control pipe is created **without** `FILE_FLAG_FIRST_PIPE_INSTANCE` and with `D:P(A;;GA;;;AU)S:(ML;;NW;;;LW)` — i.e. GENERIC_ALL to Authenticated Users plus a Low-IL write-up label. Whether an unprivileged local process can pre-create the pipe name before the service starts, or rewrite the pipe DACL via the granted `WRITE_DAC`. | On a VM with the service installed: as a standard user, attempt `CreateNamedPipe(\\.\pipe\pm-ctrl)` before service start; on a connected handle attempt `SetSecurityInfo`. If reproducible, report upstream to Intel before shipping the service. | The decision to redistribute the PresentMon service as a prerequisite. | HIGH |
| A single game PID's `\GPU Engine` instance grammar on hybrid iGPU+dGPU and on AMD / Intel Arc: whether it appears under multiple LUIDs, and whether `engtype_3D` is present on every adapter it renders on. | During a running game on a hybrid laptop and on AMD and Arc desktops, dump every `\GPU Engine(*)` instance matching the game PID and record the full instance-name set. | The primary positive signal in game detection (sustained `engtype_3D` attributable to the foreground PID). | HIGH |
| Pipe DACL `D:P(D;;GA;;;AN)(D;;GA;;;NU)(D;;GA;;;RC)(D;;GA;;;AC)(A;;GA;;;SY)(A;;GA;;;BA)(A;;0x0012019b;;;<logonSid>)S:(ML;;NW;;;ME)` grants exactly read+write+synchronize with **no** `FILE_CREATE_PIPE_INSTANCE`, `WRITE_DAC` or `WRITE_OWNER`; that deny-before-allow ordering survives `ConvertStringSecurityDescriptorToSecurityDescriptorW`; and that a remote SMB client is rejected. | Create the pipe, read the resulting DACL back with `GetSecurityInfo` + `ConvertSecurityDescriptorToStringSecurityDescriptorW` and diff against intent; attempt connection from a Low-IL process, from a second user's session, and over `\\host\pipe\...` from another machine. | Pre-emptive: the IPC boundary any future elevated helper must satisfy. **No helper ships in v1.** | HIGH |
| `SHQueryUserNotificationState` return value for a **borderless-fullscreen DXGI flip-model** game on Win11 24H2+ (`QUNS_BUSY` vs `QUNS_ACCEPTS_NOTIFICATIONS`). | Run one title in exclusive fullscreen, borderless fullscreen and windowed; poll and record the value in each mode on 23H2, 24H2 and 25H2. | Fullscreen-mode classification in the detector, and whether the `GetWindowRect`/`GetMonitorInfo` geometry fallback is mandatory rather than optional. | MEDIUM |
| `GetLastInputInfo` is **not** advanced by gamepad/controller input, so controller-only play reaches the display-sleep timeout. | With an Xbox controller on a fullscreen game, poll `GetLastInputInfo` while using only the controller; confirm the idle time grows and that the display blanks at the configured timeout. | The only justification for the display power-request feature; without it, that feature has no cause to exist. | MEDIUM |
| The `HKCU\...\Run` entry's interaction with Task Manager -> Startup "Disabled" (the undocumented `StartupApproved` state): that FrameDoctor does not launch when disabled there, and that we never repair or overwrite that state. | Add the Run value, disable the entry in Task Manager, reboot and confirm no launch; then launch manually and confirm both the Run value and the `StartupApproved` data are untouched. | The startup mechanism, and the "never re-create a startup entry the user removed" rule. | MEDIUM |
| The two-process split (Engine + Shell) actually meets the budget: ≤ 1.0 % total CPU and ≤ 90 MB working set with the Shell closed, ≤ 3.5 % and ≤ 260 MB with it open. Measured on Linux only as allocation counts, never as Windows working set. | Run a 30-minute game session with the Shell closed, then open; sample the Engine and the whole WebView2 process tree with `Get-Counter` / VMMap at 1 Hz. | The entire process-structure decision (ADR: architecture). If the Engine alone exceeds 90 MB, the split does not buy what it costs. | CRITICAL |
| Closing the Shell window actually tears down the whole WebView2 process tree (browser + renderer + GPU), rather than leaving orphans resident. | Close the Shell to tray; enumerate child processes of the Engine and any `msedgewebview2.exe` still running after 30 s. | The ≤ 90 MB UI-hidden budget line, which is only reachable if the web view is genuinely gone. | HIGH |
| Named-pipe IPC throughput and allocation on Windows. The 115 069 msg/s, 0.0 B/msg figure was measured on **Linux**, where .NET named pipes are Unix domain sockets — a different kernel object from a Windows named pipe. | Run the same probe (16-byte header + fixed-layout records, dedicated writer thread, synchronous `Write(ReadOnlySpan<byte>)`) between two processes on Windows; assert ≥ 10× the 120 msg/s budget and zero steady-state allocation. | The IPC transport and codec decision, and the zero-allocation clause of the performance budget. | HIGH |
| The Engine's pipe DACL actually scopes the pipe to one user in one session, and `PIPE_REJECT_REMOTE_CLIENTS` rejects an SMB client. | Create the pipe with the chosen SDDL; attempt to connect from a second local user, from an elevated process, and over `\\<host>\pipe\...` from another machine. | Invariant 7 (no network listener reachable off-machine) and the trust boundary of every message the Engine accepts. | CRITICAL |
| The Engine can own a tray icon and receive `WM_POWERBROADCAST`, `WM_WTSSESSION_CHANGE` and `WM_QUERYENDSESSION` from a message-only window via P/Invoke, with no WinForms and no WPF loaded. | Build the Engine with `Shell_NotifyIcon` + `CreateWindowEx(HWND_MESSAGE)`; verify the icon, its menu, and each of the three messages; measure the working-set delta against a WinForms `NotifyIcon` build. | The Engine's working-set budget and every rollback trigger in `windows-internals.md` §7.5. | HIGH |
| An MSI uninstall custom action invoking `FrameDoctor.Engine.exe --revert-all` runs to completion **before** the binaries are removed, and reverts a live applied change. | Apply the power-overlay optimization, then uninstall without closing the app; confirm the overlay is restored and the journal is empty. | Invariant 4 — uninstall must restore every applied system change. | CRITICAL |
| Killing the Shell (`TerminateProcess`) leaves the Engine collecting, detecting and writing to SQLite with no data loss; killing the Engine leaves the Shell alive and showing an honest disconnected state rather than stale numbers. | Kill each process in turn mid-session; compare the stored session against an uninterrupted control run. | The crash-isolation justification for having two processes at all. | CRITICAL |
| WebView2 Evergreen Runtime is present, or the bootstrapper installs it, on a clean Windows 10 22H2 machine with no Edge updates applied. | Clean VM, no Edge; run the installer; confirm the Shell renders. | The whole UI stack. Without the runtime the Shell is a blank window. | HIGH |
| .NET 10 self-contained, ReadyToRun, trimmed, single-file publish (16.5 MB, produced on Linux) actually starts and runs on Windows, including every P/Invoke and the LibreHardwareMonitorLib WMI paths that are documented as trim-unsafe. | Publish, then launch on Windows 10 and 11; exercise every collector; measure cold start. | The deployment model and the startup-time budget. Trimming breaks reflection paths silently. | HIGH |
| WebView2 process-group working set on Windows against the **≤190 MB** "UI visible, additional" line. Measured proxy on this box: headless Chromium 1194 with a **blank page** costs 8 processes / 277 MB PSS / 133 MB private; the FrameDoctor-shaped page costs 337 MB PSS / 176 MB private. Our own code accounts for only ~60 MB of that — the engine floor dominates. | Run the built UI on Windows 10 and 11. Record every `msedgewebview2.exe` in the group plus the WPF host, private working set and commit, from Task Manager and from the WebView2 Browser Task Manager (`OpenTaskManagerWindow`), at: Live view idle, Live view monitoring, Event Inspector open, and after a 4 h session. | The ≤190 MB memory line, and the UI-technology ADR. If it fails, the choice is Avalonia or a raised budget — not a smaller React bundle. | HIGH |
| Whether the WebView2 **GPU process** measurably steals frame time from the game while the Live view is visible. This is invariant 8 turned on the UI itself: a Chromium compositor on the same GPU as the game. | Run the A/B frame-time protocol from `performance-budget.md` three ways: FrameDoctor off; monitoring with the UI closed to tray; monitoring with the Live view visible and fullscreen-windowed gameplay. Compare Δp99 frame time and Δ1 % low. | Invariant 8, and whether the Live view may be shown during gameplay at all or is a between-sessions surface. | CRITICAL |
| Chart draw budgets measured in headless Chromium on Linux reproduce inside WebView2 on Windows, including on integrated graphics and with the GPU process unavailable (software compositing fallback). Measured here: uPlot 4096 pts p95 **1.3 ms**; hand-written Canvas 2D min/max p95 **0.5 ms** (0.3 ms in the live rAF loop); 9 synchronized panels mount in p50 **10.3 ms**. | Run the same `performance.measure` harness inside the shipped WebView2 on: a discrete-GPU desktop, an integrated-graphics laptop, and with `--disable-gpu` forced. Assert the ≤3 ms and ≤120 ms lines. | The ≤3 ms chart-draw and ≤120 ms inspector-open lines, and the choice of uPlot over a native chart stack. | MEDIUM |
| UI-process cold start on Windows: WebView2 environment creation + controller creation + navigate + first chart painted. Measured proxy here (page load only, engine already warm): DOMContentLoaded 104–189 ms, first canvas 214–376 ms. The WebView2 *process-group* spin-up is not included and is the unknown. | From a cold boot, restore the UI from the tray and time from click to first painted chart, five times, on an HDD and an SSD machine. | The "UI process exits when minimized to tray" design, which is what makes the ≤70 MB backend line reachable. If restore costs >1.5 s the UI must be suspended (`TrySuspendAsync`) rather than exited. | MEDIUM |
| Native window chrome behaviours the WPF shell owns around the WebView2 content area: per-monitor DPI v2 rescale while the Live view is rendering, snap/Aero-snap, minimize-to-tray and restore, and the WebView2 not swallowing system keyboard shortcuts. | Drag the window between a 100 % and a 175 % display mid-session; snap left/right; minimize to tray and restore; press Alt+F4, Win+Arrow, Alt+Tab with focus inside the web content. | Every claim that this is "premium Windows desktop software" rather than a browser in a frame. | MEDIUM |
| Per-**write-operation** cost on Windows with a filesystem minifilter (Defender real-time protection) in the path. The 3.8 us/4 KB buffered-write figure that justifies the write-operations budget line was measured on Linux/ext4, where no AV filter sees the write; on Windows every write is an `IRP_MJ_WRITE` a minifilter may inspect. | Run the append-log writer at 3, 120 and 625 writes/min with Defender real-time protection on and off; record per-write p50/p99 and the A/B frame-time delta for each. | The `write operations <= 240/min` line in `docs/architecture/performance-budget.md`, and whether write-op count is a cost proxy on Windows at all. | MEDIUM |
| `FlushFileBuffers` latency on NTFS/NVMe **while a game is streaming assets**. The fsync budget line is derived from a Linux `fdatasync` measurement (220 us p50, 375 us p99, 2.8 ms max) on virtio/ext4, with an idle device. | Call `FlushFileBuffers` on the session store at 1/min during a deterministic in-game benchmark; record the latency distribution and the A/B frame-time delta against a run with zero flushes. | The `fsync/FlushFileBuffers <= 2/min` line — the only line in the disk budget that governs a call which blocks on the device. | HIGH |
| That `GetProcessIoCounters().WriteOperationCount` increments **once per `WriteFile`** and not once per device I/O, and that `WriteTransferCount` counts bytes handed to the cache manager rather than bytes reaching the disk. The whole replacement disk budget is unenforceable if its instrument measures something other than what the line says. | Issue a known count of writes of known sizes (3 x 64 KB, 625 x 4 KB) and compare the counter deltas against the issued calls and bytes; repeat with `FILE_FLAG_WRITE_THROUGH`. | Every line in the Disk section of `docs/architecture/performance-budget.md` and its enforcement path. | HIGH |
| That SQLite's Win32 VFS issues the same **two `WriteFile` calls per WAL page** as the Unix VFS. Verified at source level in the 3.45.1 amalgamation (`walWriteOneFrame` sits above the VFS and calls `sqlite3OsWrite` twice; `winWrite` issues one `WriteFile` per call and never coalesces) but never executed against `winWrite`. | Process Monitor or an ETW file-I/O trace over six commits of a 200 KB blob at `page_size` 4096 and 65536; confirm 2 writes per page and the 1.05x / 1.60x byte amplification measured on Linux. | Whether SQLite is admissible as the session store container under the disk budget. | MEDIUM |
| **`SYSTEM_PROCESSOR_PERFORMANCE_INFORMATION.Reserved1[0]` is `DpcTime` and `Reserved1[1]` is `InterruptTime`.** Microsoft documents both only as `Reserved1` and describes nothing about them. The whole DPC/ISR diagnosis rests on this mapping. | Sample the struct at 2 Hz for 60 s under a synthetic DPC load (e.g. heavy network/USB traffic) alongside `\Processor Information(_Total)\% DPC Time` and `% Interrupt Time` from PDH. Require the derived and counter values to agree within 2 percentage points. | `docs/research/collector-implementation.md` §3; `cpu.dpc.time` / `cpu.isr.time`, and the "a driver is eating your frame time" diagnosis, which is the flagship undiagnosable case in `telemetry-model.md`. | HIGH |
| The **default (non-`--v1_metrics`, non-`--v2_metrics`) PresentMon 2.5.1 CSV header** is byte-identical to the 26-column row derived from `CsvOutput.cpp`, and `--track_etw_status` adds **no** columns on that code path while still emitting `[ETW Status]` on **stdout** once per second. | Run the pinned invocation against a real title; capture stdout to a file; diff the first line against the expected header string; confirm `[ETW Status]` lines are interleaved among CSV rows on stdout and absent from stderr. | The entire CSV parser and `TelemetryHealth` for the v1 frame source. A schema mismatch means zero frames, silently. | CRITICAL |
| **`FrameDropped` derived as `MsUntilDisplayed == "NA"`.** The default vocabulary has no `Dropped` column; this predicate is inferred from the row writer's `NA` condition, not from upstream documentation. | Replay an ETL with a known number of dropped frames through `PresentMon --etl_file` with the pinned flags; count rows where `MsUntilDisplayed` is `NA` and compare against the same ETL consumed through the SDK's `PM_METRIC_DROPPED_FRAMES`. | `frame.dropped`, one of the three series stutter classification consumes (`telemetry-model.md:113-119`). | HIGH |
| **`MsCPUBusy` / `MsCPUWait` / `MsBetweenAppStart` write `0` for a missing value, not `NA`** (`WriteMetricOrZero`). A real zero and a missing value are indistinguishable in the CSV. | On an OpenGL or Vulkan title (`PresentRuntime = Other`), capture 10 000 rows and record how often the triple is all-zero while `MsBetweenPresents` is non-zero. Confirm the suppression rule does not discard legitimate data on a DXGI title. | Invariant 9 and `frame.time`. Treating a sentinel zero as a measurement would report a 0 ms frame time. | HIGH |
| Every counter path string in `collector-implementation.md` §2.6 actually resolves on real hardware. All are `[unverified]`: Microsoft publishes no normative counter-name list. | Run the startup probe (`PdhAddEnglishCounterW` -> 2x `PdhCollectQueryData` -> `PdhGetFormattedCounterValue`) for every path in the table on Windows 10 22H2 and Windows 11 24H2, on Intel, AMD, NVIDIA and Arc hardware. Record which resolve and which return `PDH_CSTATUS_NO_COUNTER` / `PDH_CSTATUS_NO_OBJECT`. | Every Tier 0 metric sourced from PDH. `PdhAddEnglishCounterW` returns `ERROR_SUCCESS` for a non-existent instance, so nothing but this probe proves a path works. | CRITICAL |
| `PDH_FMT_NOCAP100` is `0x8000`, `PDH_FMT_NOSCALE` is `0x1000` and `PDH_FMT_1000` is `0x2000`. Microsoft documents the names but not the values, and **win32metadata does not contain them** (only `PDH_FMT_LONG/DOUBLE/LARGE`). | Read the values from `pdh.h` in the installed Windows SDK; confirm `% Processor Utility` returns > 100 under turbo with the flag set and exactly 100 without it. | `cpu.load.total`, `cpu.load.core` and the `% Processor Performance` effective-clock derivation. Without `NOCAP100`, every boosting CPU reads as pinned at 100 %. | HIGH |
| The **effective-clock derivation** `baseMHz x (% Processor Performance / 100)`. `docs/research/hardware-telemetry.md` cited a Microsoft page for it; the current revision of that page does not contain the claim. | On Intel HWP and AMD PBO parts, compare the derived value against a known-good tool (HWiNFO, ThrottleStop) across idle, single-core boost and all-core load. Require agreement within 5 %. | `cpu.clock.effective`, which is the whole Tier 0 CPU story after ADR 0002 removed the thermal claim. | HIGH |
| `CallNtPowerInformation(ProcessorInformation)` returns a **live** `CurrentMhz` rather than the nominal clock, and a correct per-logical-processor `MaxMhz` for the effective-clock base. | Call it at idle and under sustained all-core load on Intel HWP (12th gen+) and AMD PBO; compare `CurrentMhz` against a known-good tool and `MaxMhz` against the CPU's published base clock. | `cpu.clock` and the `baseClockMHz` input to `cpu.clock.effective`. Avoids WMI, which is not trim-safe. | HIGH |
| `PERFORMANCE_INFORMATION.CommitTotal` / `CommitLimit` are in **pages**, requiring multiplication by `PageSize`, not bytes. | Compare `CommitTotal x PageSize` against `\Memory\Committed Bytes` from PDH on the same tick. Require agreement within 1 %. | `mem.committed` and `mem.commit.limit`. A missing x4096 produces a plausible-looking number that is wrong by three orders of magnitude - the worst failure class we can ship. | HIGH |
| `NtQuerySystemInformation(SystemProcessorPerformanceInformation)` on machines with **more than 64 logical processors** returns entries only for the caller's processor group. | On a >64-thread machine, compare the returned `ReturnLength` against `48 x GetActiveProcessorCount(ALL_PROCESSOR_GROUPS)` and against `48 x GetActiveProcessorCount(0)`. | Per-core CPU load, DPC/ISR and active-core count on high-core-count workstations. Wrong here means half the machine is invisible. | MEDIUM |
| `nvmlMemory_v2_t.version` computed at runtime as `sizeof(struct) \| (2 << 24)`. The `NVML_STRUCT_VERSION` macro is function-like and absent from generated bindings. | Call `nvmlDeviceGetMemoryInfo_v2` with the computed version on an RTX 30/40/50 part; confirm `NVML_SUCCESS` and a `total` matching `nvmlDeviceGetMemoryInfo` (v1). | `gpu.vram.used` / `gpu.vram.total` on the v2 path. v1 works without a version field, so the fallback is safe - this row governs whether we need v2 at all. | LOW |
| **NVML read queries succeed unelevated.** NVIDIA documents `NVML_ERROR_NO_PERMISSION` for write operations; no primary statement was found that reads are unprivileged. | From a standard (non-admin) account, call `nvmlInit_v2`, `nvmlDeviceGetTemperature`, `nvmlDeviceGetPowerUsage`, `nvmlDeviceGetClockInfo` and `nvmlDeviceGetCurrentClocksEventReasons`; record the return code of each. | The entire Tier 0 GPU story and ADR 0002's "no elevated component" claim. If NVML reads need elevation, the flagship GPU thermal diagnosis is gated behind UAC. | HIGH |
| `nvmlDeviceGetCurrentClocksEventReasons` is exported by the **shipping GeForce driver**, not only by datacenter drivers, and returns bits other than `0x0` under real thermal or power limits. | On an RTX 30/40/50 consumer part, run a sustained GPU load with the fan curve reduced; poll the bitmask at 1 Hz and record which bits appear. Repeat with a lowered power limit. | `gpu.throttle.reason`, described in ADR 0002 as "the single highest-value signal in this entire document for thermal diagnosis". | CRITICAL |
| Loading `nvml.dll` with `LOAD_LIBRARY_SEARCH_SYSTEM32` alone succeeds on current NVIDIA drivers, i.e. the `%ProgramW6432%\NVIDIA Corporation\NVSMI` fallback is genuinely a fallback and not the primary path. | On a clean install of a current GeForce driver, attempt `LoadLibraryExW(L"nvml.dll", NULL, LOAD_LIBRARY_SEARCH_SYSTEM32)`; record success and the resolved module path. Repeat on a driver from the era that stopped copying to System32. | The DLL-planting-safe load strategy. If the System32 path fails on current drivers we must load an absolute path, and a bare-name search is not acceptable. | MEDIUM |
| The **IGCL boolean limit indicators** (`gpuPowerLimited`, `gpuTemperatureLimited`, `gpuCurrentLimited`) in `ctl_power_telemetry_t` are populated on consumer Arc hardware and change under real thermal or power limits. | On an Arc desktop GPU, run a sustained load under reduced cooling and poll `ctlPowerTelemetryGet` at 1 Hz; record which booleans assert. | `gpu.throttle.reason` on Intel, and the claim in `collector-implementation.md` §4.5 that Intel has a usable throttle-reason surface. | MEDIUM |
| The WinEvent hook, the message-only window, `SHQueryUserNotificationState` polling and `GetForegroundWindow` reconcile all coexist on **one** Engine thread with one message pump, and the `GCHandle`-pinned callback survives a full GC. | Run the Engine for 4 hours with forced `GC.Collect(2, Forcing, blocking: true)` every 30 s while switching foreground windows; confirm every `EVENT_SYSTEM_FOREGROUND` is observed and no access violation occurs. | Game detection, and the Engine's single-thread message-pump design. A collected WinEventProc delegate is a hard crash inside user32, not a managed exception. | HIGH |
| `WinVerifyTrust` with `WTD_CACHE_ONLY_URL_RETRIEVAL \| WTD_REVOCATION_CHECK_NONE` makes **no network request**, and completes in bounded time on a large (200 MB+) game executable. | Run the signer check on a 200 MB signed executable with the network cable unplugged and with a packet capture running; record wall time and confirm zero outbound connections. | Invariant 7 (local-only, offline). A default `WinVerifyTrust` attempts CRL/OCSP retrieval, which we are not permitted to make and which can block for seconds on the detection path. | HIGH |
| **`--track_etw_status` remains a hidden, supported option** across PresentMon patch releases, and the `[ETW Status]` line format (including the stray comma before `OverflowedPresents`) is stable. | Pin the bundled binary to 2.5.1 and assert the parse in a Windows integration test. On any PresentMon upgrade, re-run before shipping. | `TelemetryHealth` for the v1 frame source. Losing it means dropped ETW events are reported to the user as real stutters. | MEDIUM |
| The `SYSTEM_PROCESS_INFORMATION` prefix FrameDoctor declares matches the running kernel's, field for field, through `KernelTime`/`UserTime`/`ImageName`/`UniqueProcessId`. The structure is undocumented and has gained fields between Windows releases. | On Windows 10 22H2 and Windows 11 23H2/24H2, enumerate and compare each process's `KernelTime + UserTime` against `GetProcessTimes` on the same pid, and each `ImageName` against `Process.ProcessName`. Any mismatch means the offsets are wrong on that build. | `NtProcessAttributionSource` — the evidence that names the process responsible for a stutter. Wrong offsets read adjacent fields as CPU time and would name an arbitrary process with confidence. | CRITICAL |
| PresentMon 2.5.1's CSV column order on the pinned invocation is exactly the 26 columns `PresentMonCsvParser.PinnedHeader` declares. | Run the bundled binary against a game with the pinned arguments and assert the first output line equals `PinnedHeader` byte for byte. Repeat on every PresentMon upgrade before shipping. | The frame source. A shifted column silently reads a different metric as the frame time and produces a plausible chart of the wrong thing. The parser refuses non-matching headers, so the failure is loud — but the pin itself is unverified. | HIGH |
| `MsUntilDisplayed == NA` really is equivalent to a dropped frame on the default metric vocabulary, which has no `Dropped` column. | Capture an ETL with known dropped frames (forced with a low frame cap and VRR off), replay it through the CLI, and compare the derived predicate against `--v1_metrics`' explicit `Dropped` column. | `FrameDropped`. If the equivalence is wrong, either drops go unreported or ordinary frames are reported as dropped. | MEDIUM |
| `GetPerformanceInfo` reports commit charge in pages and its `PageSize` field is the right divisor on a machine with large pages enabled. | Read `CommitTotal * PageSize` and compare against `\Memory\Committed Bytes` from PDH on the same machine, with and without large-page support enabled. | `MemorySensorSource`. Confusing pages for bytes understates commit charge by ~256x, which is small enough to look plausible and would hide every paging problem there is. | HIGH |
| Per-process CPU sampled *after* an event is close enough to the event to be usable evidence at all. The two enumerations happen once the stutter has already been detected. | Inject a known CPU hog at a known instant during a captured session; measure the delay from the stutter to the first attribution sample and whether the hog is still identifiable in it. | `NtProcessAttributionSource` and ADR 0002's event-driven widening. If the offender is routinely gone by then, the design has to change to a continuous low-rate ring. | HIGH |
| `GetProcessInformation(ProcessPowerThrottling)` returns the *current* mask for a same-user process opened with `PROCESS_QUERY_LIMITED_INFORMATION`, and the shipped default really is `ControlMask = 0` rather than an explicit un-throttled state. | On Windows 10 22H2 and 11, read a freshly launched user process and confirm both masks are zero. Set EcoQoS via Task Manager's Efficiency mode and confirm the read reflects it. | `EcoQosChange.Read`, and with it the whole apply protocol: the captured original is what a restore writes back, and capturing "explicitly not throttled" where the truth is "system managed" would write an explicit choice the user never made. | CRITICAL |
| `OpenProcess` returning `ERROR_INVALID_PARAMETER` (87) is a reliable signal that the process is gone, distinct from an access failure. | Open a handle to a pid that has exited and to a pid owned by another user; record `GetLastError` for each. | The reconcile decision. Confusing the two either leaves journal entries forever or, worse, treats an unreadable value as a vanished target and drops the record of a change still applied. | HIGH |
| A process's start time is stable and readable for the whole life of the process, so a target string genuinely distinguishes two processes that reuse one id. | Launch and exit processes until a pid is reused; confirm the two targets differ and that reconciliation on the stale one reports the target as gone rather than restoring onto the new process. | `EcoQosState.TargetFor`. This is the guard against restoring a captured value onto an innocent process, which the compare-and-restore table cannot catch. | CRITICAL |
| `FileStream.Flush(flushToDisk: true)` followed by `File.Move` gives the journal-before-apply ordering real durability on NTFS with write caching enabled. | Write a journal entry, cut power to the machine (or use a filter driver that drops cached writes), and confirm the entry is present on reboot. | The invariant that makes power loss survivable: no reachable state in which the mutation is applied and its journal entry is absent. If the flush does not reach the device, the ordering guarantee is decorative. | CRITICAL |
| The video-encode signal (`engtype_VideoEncode` attributable to a pid) is actually available and populated while OBS or the Xbox Game Bar is recording. | Record with OBS (NVENC and x264) and with Game Bar; poll the GPU Engine counters and confirm the encoding pid is identifiable in each case. | The never-restrain-a-recording rule, which is the single most likely way this feature hurts someone. The name list is the fallback and it will not catch a renamed or unlisted encoder. | CRITICAL |

## Resolved

| What was unverified | How it was resolved | Date |
|---|---|---|
| WPF compiles from a Linux host | Built a scratch `net8.0-windows` WPF project with `EnableWindowsTargeting=true`; `dotnet build -c Release` succeeded, 0 warnings | 2026-08-23 |
